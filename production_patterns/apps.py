from django.apps import AppConfig


class ProductionPatternsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'production_patterns'
    verbose_name = 'Production Patterns'

