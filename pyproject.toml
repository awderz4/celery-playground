[project]
name = "celery-playground"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "celery>=5.6.2",
    "celery-types>=0.24.0",
    "django>=6.0.2",
    "redis>=7.1.1",
]
